name: MyTestWorkflow

on: 
  push:
    branches:
    - main

jobs:
  my-1st:
    runs-on: ubuntu-latest

    steps:
    - name: 1st Jobe
      run: echo "Starting 1st job" && cat /etc/os-release
      
  my-2nd:
    runs-on: ubuntu-latest
    needs: [my-1st]

    steps:
    - name: 2nd Jobe
      run: echo "Starting 2nd job" && ls -la /
